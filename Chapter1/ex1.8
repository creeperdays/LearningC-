#include <stdio.h>

main() {
    int c; 

    int count = 0; 

    c = getchar(); 

    while ((c = getchar()) != EOF)
    {
        if (c == ' ') {
            while ((c = getchar()) == ' ') {

            }
            putchar(' '); 
        }
    }
    




}